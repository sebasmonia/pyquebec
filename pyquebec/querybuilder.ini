[QueryOptions]
UseSchema=True

[QueryTemplates]
select=SELECT {0}
from= FROM {0} WITH (NOLOCK)
inner_join= INNER JOIN {0} ON {1} = {2}
left_join= LEFT JOIN {0} ON {1} = {2}
where= WHERE {0}
order_by = ORDER BY {0}
