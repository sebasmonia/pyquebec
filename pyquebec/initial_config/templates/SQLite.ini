[Configuration]
Engine_Name=SQLite
Uses_Schema=False
Cache_Location=

[QueryTemplates]
select=SELECT {0}
from= FROM {0} 
inner_join= INNER JOIN {0} ON {1} = {2}
left_join= LEFT JOIN {0} ON {1} = {2}
where= WHERE {0}
order_by = ORDER BY {0}

[SchemaQueries]
Schema=None
Tables=SELECT Name, 'dbo' as 'Schema' FROM sqlite_master WHERE type='table'
